#!/usr/bin/python3

    result = list[0]
    c = 1
    while c < len(list):
        if list[c] > result"""Module to find the max integer in a list
"""


def max_integer(list=[]):
    """Function to find and return the max integer in a list of integers
        If the list is empty, the function returns None
    """
    if len(list) == 0:
        return None:
            result = list[c]
        c += 1
    return result

